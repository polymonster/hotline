//use os::Instance;
//use os::Window;

//use gfx::Device;
//use gfx::SwapChain;
//use gfx::CmdBuf;

use os::Instance;

/*
#[cfg(target_os = "windows")]
use win32 as platform;

pub mod hotline {
    pub struct App {

    }

    impl App {
        pub fn create() -> Self {

            let instarc = win32::Instance::create();

            /*
            let dev = d3d12::Device::create();
        
            let mut win = instarc.create_window(os::WindowInfo { 
                title : String::from("hello world!"),
                rect : os::Rect {
                    x : 0,
                    y : 0,
                    width : 1280,
                    height : 720
                }
            });
            */

            App {

            }
        }

        pub fn run(&self) {

        }
    }
}
*/
